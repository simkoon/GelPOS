let mongoose,Schema;_521‍.x([["default",()=>_521‍.o]]);_521‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["Schema",["Schema"],function(v){Schema=v}]]);

const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String], // 문자열로 이루어진 배열
  publishedDate: {
    type: Date,
    default: Date.now, // 현재 날짜를 기본 값으로 지정
  },
  user: {
    _id: mongoose.Types.ObjectId,
    userid: String,
    username: String,
    email: String,
  },
});

const Post = mongoose.model('Post', PostSchema);
_521‍.d(Post);
